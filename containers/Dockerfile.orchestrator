FROM python:3.12-slim

WORKDIR /workspace
COPY requirements requirements
RUN pip install --no-cache-dir -r requirements/orchestrator.txt
COPY src src
ENV PYTHONPATH=/workspace/src
ENTRYPOINT ["python", "-m", "football_pose"]
