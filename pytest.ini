[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    gpu: requires the configured GPU server and real model containers
    integration: exercises filesystem, video, subprocess, or archive boundaries
